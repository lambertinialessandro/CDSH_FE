import en from './en';
import de from './de';
/* import es from './es';
import it from './it'; */

const ns_common = 'common';

export const resources = { en, de /* , es, it */ };

export const defaultNS = ns_common;
export const ns = [ns_common];

export const supportedLngs = ['en', 'de' /* , 'es', 'it' */];
export const fallbackLng = 'en';
