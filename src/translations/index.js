import en from './en';
import it from './it';

const ns_common = 'common';

export const resources = { en, it };

export const defaultNS = ns_common;
export const ns = [ns_common];

export const supportedLngs = ['it', 'en'];
export const fallbackLng = 'it';
