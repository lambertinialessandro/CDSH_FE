






TODO LIST

2) return [{ name: 'All' }, ...uniqueCategories.map((cat) => ({ name: cat }))]; // TODO: ALL ? no!

3) StudentSelector, TeamSelector, SubjectSelector => translations

6) all of this
 if (loading) return <Box sx={{ p: 10, textAlign: 'center' }}>Laden...</Box>;
  if (error || !selectedMember) return <Box sx={{ p: 10, textAlign: 'center' }}>Mitglied nicht gefunden.</Box>;


